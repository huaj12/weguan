<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="Act_Mapper">
	<update id="increasePopularity" parameterType="map">
	    update tb_act set popularity = popularity + #{p} where id = #{id}
	</update>
</mapper>